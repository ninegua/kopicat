canisters:
  - name: backend
    build:
      steps:
        - type: pre-built
          path: build/backend.wasm
  - name: frontend
    recipe:
      type: "@dfinity/asset-canister@v2.1.0"
      configuration:
        dir: dist
